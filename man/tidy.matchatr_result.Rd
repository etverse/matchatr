% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tidy.R
\name{tidy.matchatr_result}
\alias{tidy.matchatr_result}
\title{Tidy a matchatr contrast result}
\usage{
\method{tidy}{matchatr_result}(x, ...)
}
\arguments{
\item{x}{A \code{matchatr_result} object returned by \code{\link[=contrast]{contrast()}}.}

\item{...}{Unused; present for generic consistency.}
}
\value{
A \code{data.table} with columns \code{term}, \code{estimate}, \code{std.error},
\code{type}, \code{conf.low}, \code{conf.high}.
}
\description{
Returns the contrasts carried by a \code{matchatr_result} as
a tidy \code{data.table}, one row per reported contrast, with the contrast scale
recorded in a \code{type} column.
}
\examples{
set.seed(1)
df <- data.frame(case = rep(c(1, 0), each = 100), x = rbinom(200, 1, 0.4))
fit <- matcha(df, outcome = "case", exposure = "x", design = unmatched_cc())
tidy(contrast(fit, type = "or"))
}
\seealso{
\code{\link[=contrast]{contrast()}}

Other tidiers: 
\code{\link{tidy.matchatr_fit}()},
\code{\link{tidy.matchatr_homogeneity}()}
}
\concept{tidiers}
