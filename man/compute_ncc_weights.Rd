% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/weights_design.R
\name{compute_ncc_weights}
\alias{compute_ncc_weights}
\title{Compute working-model inclusion probability weights for an NCC sample}
\usage{
compute_ncc_weights(
  ncc,
  cohort,
  method = c("glm", "gam"),
  selection_formula = NULL,
  time,
  entry = NULL
)
}
\arguments{
\item{ncc}{A \code{data.table} or \code{data.frame} from \code{\link[=sample_ncc]{sample_ncc()}} with
\code{incl_prob = TRUE}. Must contain the columns \code{.cohort_row} (integer cohort
row index), \code{ipw_weight}, \code{set} (matched-set id), \code{case} (per-set 0/1
indicator), and \code{risk_time} (set's event time).}

\item{cohort}{A \code{data.frame} or \code{data.table} — the \strong{full Phase-1 cohort}
from which \code{ncc} was drawn. Must contain the \code{time} column and any
covariates referenced in \code{selection_formula}. Required for working-model
methods; \code{NULL} is rejected with \code{matchatr_missing_phase1}.}

\item{method}{Character scalar; the weight estimation strategy. One of
\code{"glm"} (logistic regression via \code{\link[stats:glm]{stats::glm()}}) or \code{"gam"} (generalised
additive model via \code{\link[mgcv:gam]{mgcv::gam()}}).}

\item{selection_formula}{\code{NULL} (the default) or a one-sided formula naming
the predictors for the selection model. The response is always \code{selected}
(the binary indicator of being chosen as a control). Default \code{NULL} maps
to \code{~ risk_time}. Smooth terms (e.g. \code{s(risk_time)}) are supported only
for \code{method = "gam"}.}

\item{time}{A single character string naming the \strong{event/follow-up time}
column in \code{cohort}. Determines which cohort subjects are at risk at each
event time: a subject is eligible if \code{cohort[[time]] >= risk_time_k} (and
\code{cohort[[entry]] < risk_time_k} when \code{entry} is supplied).}

\item{entry}{\code{NULL} (everyone enters at the origin) or a single character
string naming a \strong{delayed-entry} column in \code{cohort}.}
}
\value{
A copy of \code{ncc} (as a \code{data.table}) with the \code{ipw_weight} column
set to the working-model inverse inclusion probabilities: 1 for cases,
1/π_j (>= 1) for sampled controls. The \code{.cohort_row} column is preserved
unchanged.
}
\description{
Replaces or sets the \code{ipw_weight} column of a nested case-control dataset
using a \strong{working model} for the probability that a subject is selected as
a control at each event time. Two estimation strategies are available:
\itemize{
\item \code{"glm"} — fits a logistic regression of the binary selection indicator
(\code{selected ~ selection_formula}) across all (subject, event-time) pairs
where the subject is in the risk set; uses \code{\link[stats:glm]{stats::glm()}}.
\item \code{"gam"} — same but uses \code{\link[mgcv:gam]{mgcv::gam()}} so that smooth terms
(e.g. \code{s(risk_time)}) are available in \code{selection_formula}.
}

Both methods require the \strong{full Phase-1 cohort} to reconstruct the risk set
at every event time and determine which subjects were eligible but not
selected. Omitting \code{cohort} (or supplying a cohort without the \code{time}
column) aborts with \code{matchatr_missing_phase1}.
}
\details{
The working-model inclusion probability for subject j is:

π_j = 1 − ∏_(k: j ∈ R(t_k)) (1 − p̂_jk)

where the product runs over all event times t_k at which j was in the risk
set R(t_k) (excluding the case's own failure time), and p̂_jk is the
predicted probability from the fitted selection model. Cases (cohort events)
are always included with weight 1.

The default \code{selection_formula = NULL} maps to \code{~ risk_time}, a time-only
logistic model matching the simplest GLM specification of Borgan, Samuelsen
& Aastveit (2003, \emph{Lifetime Data Analysis} 9(2)). Richer models can include
cohort covariates: \code{~ risk_time + age + sex}.

\strong{Population-stratum matching caveat.} If \code{ncc} was generated with
\code{\link[=sample_ncc]{sample_ncc()}} using a \code{match} argument (e.g. \code{match = ~ sex}), controls
were drawn only from subjects in the case's matching stratum. The default
working model does not condition on the matching variable, which causes all
at-risk subjects (including those from other strata) to be treated as
"eligible but not selected". Add the matching variable to \code{selection_formula}
(e.g. \code{selection_formula = ~ risk_time + sex}) to partially account for this.
}
\examples{
set.seed(1)
tt <- rexp(500, 0.1)
cohort <- data.frame(
  id = 1:500,
  t  = pmin(tt, 15),          # bound follow-up to avoid empty late risk sets
  d  = as.integer(tt <= 15),
  x  = rbinom(500, 1, 0.4)
)
ncc <- sample_ncc(cohort, time = "t", event = "d", m = 2, incl_prob = TRUE)

# Replace Samuelsen KM weights with GLM working-model weights
ncc_glm <- compute_ncc_weights(ncc, cohort = cohort, method = "glm", time = "t")

# Fit IPW Cox using the GLM weights
fit <- matcha(ncc_glm, outcome = "d", exposure = "x",
              design = nested_cc(strata = "set", time = "t"),
              estimator = "ipw_cox")
contrast(fit)

}
\seealso{
\code{\link[=sample_ncc]{sample_ncc()}}, \code{\link[=matcha]{matcha()}}, \code{\link[=nested_cc]{nested_cc()}}

Other sampling: 
\code{\link{reuse_ncc_endpoint}()},
\code{\link{sample_ncc}()},
\code{\link{sample_ncc_counter_matched}()}
}
\concept{sampling}
