% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tidy.R
\name{tidy.matchatr_fit}
\alias{tidy.matchatr_fit}
\title{Tidy a matchatr fit into a coefficient / odds-ratio table}
\usage{
\method{tidy}{matchatr_fit}(
  x,
  conf.int = TRUE,
  conf.level = 0.95,
  exponentiate = FALSE,
  robust = FALSE,
  ...
)
}
\arguments{
\item{x}{A \code{matchatr_fit} whose \code{model} is a fitted binomial \code{glm}.}

\item{conf.int}{Logical; add \code{conf.low} / \code{conf.high} Wald bounds. Default
\code{TRUE}.}

\item{conf.level}{Numeric confidence level in (0, 1). Default 0.95.}

\item{exponentiate}{Logical; report \code{estimate} and confidence bounds on the
odds-ratio scale. Default \code{FALSE}.}

\item{robust}{Logical; use the Huber-White sandwich standard error instead of
the model information matrix. Default \code{FALSE}.}

\item{...}{Unused; present for generic consistency.}
}
\value{
A \code{data.table} with columns \code{term}, \code{estimate}, \code{std.error},
\code{statistic}, \code{p.value}, and (when \code{conf.int}) \code{conf.low}, \code{conf.high}. For a
polytomous (\code{estimator = "polytomous"}) fit the coefficients form one
equation per non-reference outcome group, so a leading \code{y.level} column
names the group each row's contrast is against the reference.
}
\description{
Returns the fitted unmatched case-control logistic regression as a tidy
coefficient table — one row per model term — on the log-odds scale by
default, or the odds-ratio scale with \code{exponentiate = TRUE}.
}
\details{
Standard errors come from the model information matrix, or the Huber-White
sandwich when \code{robust = TRUE}. The Wald statistic and p-value are always on
the coefficient (log-odds) scale; with \code{exponentiate = TRUE} the estimate and
the confidence bounds are exponentiated while \code{std.error} stays on the
log-odds scale (the broom convention). The intercept row is included but is
not an interpretable baseline risk: under separate case / control sampling it
is offset by the log sampling-fraction ratio (Prentice & Pyke, 1979). Only the
parametric coefficients are reported: when the fit is an \code{mgcv::gam}, the
smooth-basis terms (\verb{s(age).1}, ...) are penalized basis weights, not odds
ratios, and are excluded.
}
\examples{
set.seed(1)
df <- data.frame(case = rep(c(1, 0), each = 100), x = rbinom(200, 1, 0.4))
fit <- matcha(df, outcome = "case", exposure = "x", design = unmatched_cc())
tidy(fit, exponentiate = TRUE)
}
\seealso{
\code{\link[=matcha]{matcha()}}, \code{\link[=contrast]{contrast()}}, \code{\link[=summary.matchatr_fit]{summary.matchatr_fit()}}

Other tidiers: 
\code{\link{tidy.matchatr_homogeneity}()},
\code{\link{tidy.matchatr_result}()}
}
\concept{tidiers}
