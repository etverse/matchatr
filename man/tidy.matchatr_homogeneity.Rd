% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/homogeneity.R
\name{tidy.matchatr_homogeneity}
\alias{tidy.matchatr_homogeneity}
\title{Tidy a matchatr homogeneity test}
\usage{
\method{tidy}{matchatr_homogeneity}(x, ...)
}
\arguments{
\item{x}{A \code{matchatr_homogeneity} object returned by \code{\link[=test_homogeneity]{test_homogeneity()}}.}

\item{...}{Unused; present for generic consistency.}
}
\value{
A \code{data.table} with columns \code{term}, \code{common_or}, \code{conf.low},
\code{conf.high}, \code{statistic}, \code{df}, and \code{p.value}.
}
\description{
Returns the per-exposure homogeneity results carried by a
\code{matchatr_homogeneity} object as a tidy \code{data.table}, one row per exposure
term, with the common (pooled) odds ratio, its confidence bounds, and the
homogeneity Wald chi-squared statistic, degrees of freedom, and p-value.
}
\examples{
set.seed(5)
n <- 2000
x <- rbinom(n, 1, 0.4)
eta <- cbind(control = 0, A = -1 + log(2) * x, B = -1.4 + log(2) * x)
prob <- exp(eta) / rowSums(exp(eta))
g <- apply(prob, 1, function(p) sample(c("control", "A", "B"), 1, prob = p))
d <- data.frame(g = g, x = x)
fit <- matcha(d, outcome = "g", exposure = "x",
              design = unmatched_cc(), estimator = "polytomous",
              reference = "control")
tidy(test_homogeneity(fit))
}
\seealso{
\code{\link[=test_homogeneity]{test_homogeneity()}}

Other tidiers: 
\code{\link{tidy.matchatr_fit}()},
\code{\link{tidy.matchatr_result}()}
}
\concept{tidiers}
