% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/absolute_risk.R
\name{absolute_risk}
\alias{absolute_risk}
\alias{absolute_risk.matchatr_fit}
\title{Absolute risk from a sampled-cohort survival fit}
\usage{
absolute_risk(fit, ...)

\method{absolute_risk}{matchatr_fit}(fit, newdata, times, conf_level = 0.95, ...)
}
\arguments{
\item{fit}{A \code{matchatr_fit} object returned by \code{\link[=matcha]{matcha()}}. The case-cohort
(\code{"cch"}), IPW nested case-control weighted Cox (\code{"ipw_cox"}), and IPW nested
case-control Weibull AFT (\code{"ipw_aft"}) engines are supported.}

\item{...}{Unused; present for S3 consistency.}

\item{newdata}{A data frame of covariate values at which to evaluate F_x(t).
Must contain columns matching the exposure and confounders used in \code{fit}.
Each row is one covariate pattern; the result table carries a \code{row} column
that indexes back to \code{newdata}.}

\item{times}{Non-empty numeric vector of evaluation times. Duplicates are
dropped; times are sorted before evaluation. Times before the first event
return F̂ = 0; times after the last event return the last Breslow value
(step-function extrapolation).}

\item{conf_level}{Numeric in (0, 1). Confidence level for the pointwise
intervals. Default \code{0.95}.}
}
\value{
A \code{matchatr_absolute_risk} object. See \code{absolute_risk.matchatr_fit}
for details on the return structure.

A \code{matchatr_absolute_risk} list with elements:
\itemize{
\item \verb{$estimates}: a \code{data.table} with columns \code{row} (newdata row index),
\code{time}, \code{estimate} (F̂_x(t)), \code{ci_lower}, \code{ci_upper} (delta-method CI
on the probability scale).
\item \verb{$times}: the sorted evaluation times.
\item \verb{$newdata}: the supplied covariate patterns.
\item \verb{$conf_level}, \verb{$ci_method}, \verb{$engine}, \verb{$estimator}, \verb{$method}.
}
}
\description{
Estimates the cumulative incidence F_x(t) from a fitted survival design. Three
engines are supported: the case-cohort pseudo-likelihood (\code{"cch"}, Borgan &
Liestøl 1990) and the IPW nested case-control weighted Cox (\code{"ipw_cox"},
Samuelsen-weighted Breslow over the reused controls) both build
F_x(t) = 1 − exp(−exp(β̂ᵀ x) Λ̂₀(t)) from an inverse-probability-weighted (IPW)
Breslow cumulative baseline hazard Λ̂₀(t); the IPW nested case-control
parametric AFT (\code{"ipw_aft"}, any of the Weibull / exponential / lognormal /
loglogistic baselines) builds F_x(t) = G((log t − η̂)/σ̂) directly from the
fitted survival curve, where η̂ is the AFT linear predictor, σ̂ the scale, and G
the baseline error CDF. Pointwise confidence intervals use the delta method:
for the Cox-type engines and the extreme-value AFT baselines this is the
complementary log-log scale inverted to the risk scale; for the lognormal /
loglogistic baselines it is the Wald interval on the standardised residual
mapped through G.
}
\examples{
\dontrun{
fit <- matcha(cohort, outcome = "d", exposure = "x",
              design = case_cohort(subcohort = "sc", time = "t"),
              confounders = ~z, estimator = "cch")
absolute_risk(fit, newdata = data.frame(x = 1, z = 0), times = c(1, 2, 3))
}
}
\seealso{
\code{\link[=matcha]{matcha()}}, \code{\link[=contrast]{contrast()}}, \code{\link[=case_cohort]{case_cohort()}}, \code{\link[=sample_ncc]{sample_ncc()}}

Other contrasts: 
\code{\link{contrast}()},
\code{\link{excess_risk}()},
\code{\link{print.matchatr_absolute_risk}()},
\code{\link{print.matchatr_excess_risk}()},
\code{\link{tidy.matchatr_absolute_risk}()},
\code{\link{tidy.matchatr_excess_risk}()}
}
\concept{contrasts}
