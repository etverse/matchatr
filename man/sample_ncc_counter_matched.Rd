% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/risk_set_sampling.R
\name{sample_ncc_counter_matched}
\alias{sample_ncc_counter_matched}
\title{Draw a counter-matched nested case-control sample from a cohort}
\usage{
sample_ncc_counter_matched(
  cohort,
  time,
  event,
  surrogate,
  m = 1L,
  match = NULL,
  entry = NULL
)
}
\arguments{
\item{cohort}{A data.frame or data.table with one row per subject.}

\item{time}{A single character string naming the exit / event-time column.
Must be numeric.}

\item{event}{A single character string naming the event indicator (logical,
two-level factor, or numeric 0/1); at least one event must occur.}

\item{surrogate}{A single character string naming the binary surrogate
exposure column. Controls are drawn from subjects whose surrogate value
differs from the case's. Must be logical, numeric 0/1, or a two-level
factor; no missing values.}

\item{m}{A single whole number >= 1, the number of controls sampled per
case from the opposite surrogate stratum (default 1).}

\item{match}{\code{NULL} (no additional matching) or a one-sided formula naming
population-stratum column(s) controls must share with the case
(e.g. \code{~ sex + birth_cohort}).}

\item{entry}{\code{NULL} (everyone enters at the time origin) or a single
character string naming a delayed-entry / left-truncation column.}
}
\value{
A \code{data.table} with one row per sampled subject: the selected rows
of \code{cohort} (all original columns) plus \code{set} (integer matched-set id),
\code{case} (per-set 0/1 indicator), \code{risk_time} (the set's failure time), and
\code{log_w} (the log-sampling-weight for the weighted partial likelihood).
Aborts with \code{matchatr_empty_risk_set} when any case has no eligible
control in the opposite surrogate stratum.
}
\description{
Generates a counter-matched nested case-control (NCC) dataset from a full
cohort: at each event time the case is matched to \code{m} controls drawn
exclusively from the \emph{opposite} surrogate stratum, concentrating study
resources in subjects whose surrogate exposure differs from the case. The
analysis weights (log-sampling-weights) for the Langholz-Borgan (1995)
weighted partial likelihood are appended as a \code{log_w} column: the case
represents its entire surrogate stratum (log-weight = log(n_same + 1)) and
each control represents the opposite stratum divided by the controls drawn
(log-weight = log(n_other / m)). The result feeds straight into
\code{matcha(design = counter_matched(strata = "set", time = "risk_time", weights = "log_w"))}, whose \code{survival::coxph} fit reports the hazard ratio.
}
\details{
Controls are drawn without replacement from subjects at risk at the case's
failure time (\verb{entry < tc <= time}) in the \emph{opposite} surrogate stratum.
When fewer than \code{m} eligible controls exist (late failure times or narrow
strata), the smaller available set is returned rather than an error. A case
with no eligible control in the opposite stratum — which means the entire
at-risk population shares the case's surrogate value — aborts with
\code{matchatr_empty_risk_set}, signalling a sampling-design failure.

Additional matching (\code{match = ~ s1 + s2}) restricts each risk set to
subjects sharing the case's population-stratum values on the named
variables. The eligibility pool is first restricted by the match stratum,
and the surrogate split is then applied within that pool.

The surrogate column must be binary: logical, numeric 0/1, or a two-level
factor. Missing values abort with \code{matchatr_bad_input}.

The log-weights are the key difference from unweighted \code{sample_ncc()}: with
pure counter-matching the unweighted clogit is biased because the controls
were drawn non-uniformly (opposite-stratum only), so the weighted partial
likelihood (coxph + offset) must be used for analysis.
}
\examples{
set.seed(1)
tt <- rexp(200, 0.1)
cohort <- data.frame(
  id  = 1:200,
  t   = pmin(tt, 15),
  d   = as.integer(tt <= 15),
  x   = rbinom(200, 1, 0.4),
  z   = rbinom(200, 1, 0.5)   # binary surrogate
)
ncc_cm <- sample_ncc_counter_matched(cohort, time = "t", event = "d",
                                     surrogate = "z", m = 1L)
# Analyse: weighted partial likelihood identifies the HR.
fit <- matcha(ncc_cm, outcome = "case", exposure = "x",
              design = counter_matched(strata = "set", time = "risk_time",
                                       weights = "log_w"),
              estimator = "weighted_cox")
contrast(fit)

}
\seealso{
\code{\link[=counter_matched]{counter_matched()}}, \code{\link[=matcha]{matcha()}}, \code{\link[=sample_ncc]{sample_ncc()}}

Other sampling: 
\code{\link{sample_ncc}()}
}
\concept{sampling}
