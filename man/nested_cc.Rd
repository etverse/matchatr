% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cc_design.R
\name{nested_cc}
\alias{nested_cc}
\title{Nested case-control design}
\usage{
nested_cc(strata, time, ratio = NULL)
}
\arguments{
\item{strata}{A non-empty character vector naming the column(s) that
identify the sampled risk sets (the matched-set id).}

\item{time}{A single character string naming the event/entry time column
that defines the risk sets. Carried for the inclusion-probability weight
and weighted-Cox estimators built on this design.}

\item{ratio}{\code{NULL} or a single whole number >= 1. Controls sampled per
case.}
}
\value{
A \code{matchatr_design} object of \code{type} \code{"nested_cc"} carrying the
strata, time, and ratio, with a \code{weight_spec} flagged for
inclusion-probability weighting.
}
\description{
Declares a nested case-control (NCC) sample drawn from a cohort by
risk-set (incidence-density) sampling: at each event time, the case is
matched to \code{ratio} controls sampled from those still at risk. The classical
analysis treats each sampled risk set as a stratum in a conditional partial
likelihood (\code{survival::clogit}/\code{coxph}); because the controls are sampled
from the risk set, the conditional odds ratio equals the hazard ratio with
no rare-disease assumption.
}
\examples{
nested_cc(strata = "set", time = "t")
nested_cc(strata = "set", time = "t", ratio = 3)

}
\seealso{
\code{\link[=case_cohort]{case_cohort()}}, \code{\link[=counter_matched]{counter_matched()}}, \code{\link[=matcha]{matcha()}}

Other design constructors: 
\code{\link{case_cohort}()},
\code{\link{counter_matched}()},
\code{\link{matched_cc}()},
\code{\link{two_phase}()},
\code{\link{unmatched_cc}()}
}
\concept{design constructors}
