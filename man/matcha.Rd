% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/matcha.R
\name{matcha}
\alias{matcha}
\title{Fit a case-control-type design}
\usage{
matcha(data, outcome, exposure, design, confounders = NULL, estimator = NULL)
}
\arguments{
\item{data}{A data.frame or data.table. Not mutated; a data.table copy is
stored on the fit.}

\item{outcome}{Character scalar naming the binary case-status column (a
logical, two-level factor, or numeric 0/1 column).}

\item{exposure}{Character scalar naming the exposure column.}

\item{design}{A \code{matchatr_design} object from one of the design
constructors (\code{\link[=unmatched_cc]{unmatched_cc()}}, \code{\link[=matched_cc]{matched_cc()}}, \code{\link[=nested_cc]{nested_cc()}},
\code{\link[=case_cohort]{case_cohort()}}, \code{\link[=two_phase]{two_phase()}}, \code{\link[=counter_matched]{counter_matched()}}).}

\item{confounders}{A one-sided formula of confounders (e.g. \code{~ age + smoke}),
or \code{NULL} for an unadjusted analysis.}

\item{estimator}{Character scalar naming the analysis, or \code{NULL} to use the
design's canonical default. Classical choices are design-specific
(\code{"logistic"} / \code{"mh"} for unmatched CC, \code{"clogit"} for matched CC / NCC,
\code{"cch"} for case-cohort); the case-control-weighted causal estimators
\code{"ccw_gformula"}, \code{"ccw_ipw"}, \code{"ccw_aipw"}, \code{"ccw_tmle"} apply to any
design but require a prevalence q0 on the design.}
}
\value{
A \code{matchatr_fit} object: a list with the validated specification
(\code{data}, \code{outcome}, \code{exposure}, \code{confounders}, \code{design}, \code{estimator},
\code{engine}), a \code{details} list (resolved engine, weighting scheme, reserved
variance / weight slots, case and control counts), and the originating
\code{call}. The \code{model} slot holds the fitted estimation object for an
implemented engine, or \code{NULL} otherwise.
}
\description{
The fit verb for matchatr (mirroring \code{causatr::causat()} and
\code{survatr::surv_fit()}). \code{matcha()} takes the analysis data, the
outcome/exposure roles, a sampling \code{design} object, and an \code{estimator}, then
validates the request and resolves it to an estimation engine. The two
arguments are orthogonal: \code{design} selects the sampling structure (strata,
time, prevalence q0, inclusion weights) and \code{estimator} selects the analysis
(conditional vs marginal; odds ratio vs hazard ratio vs risk difference).
}
\details{
Weights are never read from or written to \code{data}. The design's \code{weight_spec}
records the intended scheme; the case-control weights (q0-based, Rose &
van der Laan) and design / inclusion-probability weights (Samuelsen, Borgan)
are kept in distinct slots on the fit (\code{details$cc_weights},
\code{details$design_weights}) because their variance consequences differ.

The resolved engine is run as part of the fit: an implemented estimator (the
unmatched case-control logistic regression) populates the \code{model} slot, while
an engine with no wired estimator leaves it \code{NULL}. \code{details$engine} records
the engine the (design, estimator) pair resolved to.
}
\examples{
set.seed(1)
df <- data.frame(
  case  = rep(c(1, 0), each = 50),
  x     = rbinom(100, 1, 0.4),
  age   = rnorm(100, 50, 10),
  smoke = rbinom(100, 1, 0.3),
  set   = rep(1:50, times = 2)
)

# Unmatched case-control -> conditional odds ratio (logistic)
matcha(df, outcome = "case", exposure = "x",
       design = unmatched_cc(), confounders = ~ age + smoke)

# Matched case-control -> conditional logistic regression
matcha(df, outcome = "case", exposure = "x",
       design = matched_cc(strata = "set"), estimator = "clogit")

}
\seealso{
\code{\link[=unmatched_cc]{unmatched_cc()}}, \code{\link[=matched_cc]{matched_cc()}}, \code{\link[=nested_cc]{nested_cc()}}, \code{\link[=case_cohort]{case_cohort()}}
}
