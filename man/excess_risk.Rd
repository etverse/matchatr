% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/excess_risk.R
\name{excess_risk}
\alias{excess_risk}
\alias{excess_risk.matchatr_fit}
\title{Time-varying cumulative excess hazard from an additive NCC fit}
\usage{
excess_risk(fit, ...)

\method{excess_risk}{matchatr_fit}(fit, times, conf_level = 0.95, ...)
}
\arguments{
\item{fit}{A \code{matchatr_fit} returned by \code{\link[=matcha]{matcha()}} with
\code{estimator = "ipw_aalen"}.}

\item{...}{Unused; present for S3 consistency.}

\item{times}{Non-empty numeric vector of evaluation times. Duplicates are
dropped and times sorted; times before the first event return B̂ = 0, times
after the last estimable event return the last cumulative value
(step-function extrapolation).}

\item{conf_level}{Numeric in (0, 1). Pointwise confidence level. Default
\code{0.95}.}
}
\value{
A \code{matchatr_excess_risk} object. See \code{excess_risk.matchatr_fit} for
the structure.

A \code{matchatr_excess_risk} list with elements:
\itemize{
\item \verb{$estimates}: a \code{data.table} with columns \code{term} (covariate),
\code{time}, \code{estimate} (B̂_j(t)), \code{se}, \code{ci_lower}, \code{ci_upper} (symmetric Wald
interval on the linear scale).
\item \verb{$times}: the sorted evaluation times.
\item \verb{$terms}: the reported covariate terms.
\item \verb{$conf_level}, \verb{$ci_method}, \verb{$engine}, \verb{$estimator}, \verb{$n}.
}
}
\description{
Estimates the Aalen cumulative regression functions
B_j(t) = ∫₀ᵗ β_j(s) ds — the \strong{time-varying excess cumulative hazard} for each
covariate — from a fitted IPW nested case-control additive-hazards
(\code{"ipw_aalen"}) design. Where \code{contrast(type = "excess")} reports one
time-\emph{constant} excess hazard per covariate (the Lin-Ying model), \code{excess_risk()}
relaxes the constant-effect assumption and returns the full cumulative
regression function over time — the additive analogue of \code{\link[=absolute_risk]{absolute_risk()}}.
}
\details{
The fully nonparametric additive-hazards model (Aalen 1980) writes
λ(t | x) = β₀(t) + Σ_j β_j(t) x_j with time-varying coefficients. Its weighted
least-squares estimator gives the cumulative regression functions in increments
at each event time t_i,

dB̂(t_i) = (X̃ᵀ W X̃)⁻¹ X̃ᵀ W dN(t_i),

where X̃ is the at-risk design matrix (an intercept column plus the covariates),
W = diag(w) the Samuelsen inclusion weights, and dN(t_i) the event indicator
over the risk set. B̂_j(t) = Σ over (t_i ≤ t) of dB̂_j(t_i). Pointwise variances
use the Aalen (martingale) estimator

V̂(t) = Σ over (t_i ≤ t) of (X̃ᵀ W X̃)⁻¹ (Σ over failures w_i² x̃_i x̃_iᵀ) (X̃ᵀ W X̃)⁻¹,

and the pointwise interval is B̂_j(t) ± z·sqrt(V̂_jj(t)) on the linear scale
(symmetric, possibly negative — an excess hazard is a rate difference). The
weighted estimator reuses controls via 1/π_j and is valid under nested
case-control sampling (Borgan & Langholz 1997). \code{timereg::aalen} (without
\code{const()}) computes the same \code{cum} and \code{var.cum}.

The estimator is defined only while the weighted design X̃ᵀWX̃ is non-singular;
if the risk set shrinks below full column rank at a late event time, B̂(t) is
truncated there and a \code{matchatr_truncated_excess} warning reports the last
estimable time. The intercept (baseline cumulative hazard) is not reported —
only the covariate excess-hazard functions.
}
\examples{
\dontrun{
ncc <- sample_ncc(cohort, time = "t", event = "d", m = 3, incl_prob = TRUE)
fit <- matcha(ncc, outcome = "d", exposure = "x",
              design = nested_cc(strata = "set", time = "t"),
              estimator = "ipw_aalen")
excess_risk(fit, times = c(1, 2, 3, 4))
}
}
\seealso{
\code{\link[=matcha]{matcha()}}, \code{\link[=contrast]{contrast()}}, \code{\link[=absolute_risk]{absolute_risk()}}, \code{\link[=sample_ncc]{sample_ncc()}}

Other contrasts: 
\code{\link{absolute_risk}()},
\code{\link{contrast}()},
\code{\link{print.matchatr_absolute_risk}()},
\code{\link{print.matchatr_excess_risk}()},
\code{\link{tidy.matchatr_absolute_risk}()},
\code{\link{tidy.matchatr_excess_risk}()}
}
\concept{contrasts}
