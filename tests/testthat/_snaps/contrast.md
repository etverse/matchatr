# the not-estimated message reads clearly

    Code
      contrast(fit)
    Condition
      Error in `contrast()`:
      ! This `matchatr_fit` carries no estimates to contrast.
      i Its `ccw_ipw` estimation engine has not produced a fitted model.

